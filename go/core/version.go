package core

// Version is the anyhook Go module version. It tracks the TypeScript package
// numbering; this Go port ships at M1 parity (core delivery engine + signing +
// testing harness), matching the TS packages/core, packages/signing, and
// packages/testing behavior.
const Version = "0.1.0"
